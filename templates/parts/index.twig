{% extends "_layouts/application" %}

{% block content %}
  <h1>Parts Kit</h1>

  <ul>
    {% for entry in craft.entries({ section: 'partsKit' }).all %}
      <li><a href={{ entry.url }}>{{ entry.title }}</a></li>
    {% endfor %}
  </ul>
{% endblock %}
